# URL pública do backend FastAPI.
# Local:    http://localhost:8000
# Demo:     https://abc123.ngrok-free.app   (URL gerada pelo `ngrok http 8000`)
# Produção: configurar no painel da Vercel (Project → Settings → Environment Variables)
VITE_API_BASE_URL=http://localhost:8000
